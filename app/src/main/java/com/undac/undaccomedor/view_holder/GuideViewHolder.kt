package com.undac.undaccomedor.view_holder

import android.content.Context
import android.view.View
import com.undac.undaccomedor.item.GuideItem

internal class GuideViewHolder(itemView: View, override val mApplicationContext: Context) : BaseViewHolder<GuideItem>(itemView) {

    override fun bind(item: GuideItem) {

        //Do your view assignment here from the data model
    }

}