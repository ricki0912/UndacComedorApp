package com.undac.undaccomedor.view_holder

import android.content.Context
import android.view.View
import com.undac.undaccomedor.item.ServiceItem

internal class ServiceViewHolder(itemView: View, override val mApplicationContext: Context) : BaseViewHolder<ServiceItem>(itemView) {

    override fun bind(item: ServiceItem) {
        //Do your view assignment here from the data model
    }

}